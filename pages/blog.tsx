import styles from './index.module.css';
import Layout from '@components/layout';

export default function Blog() {
    return (
        <Layout route="/blog">
            <h1>Blog</h1>
        </Layout>
    );
}

import Layout from '@components/Layout';

export default function Blog() {
    return (
        <Layout>
            <div
                style={{
                    display: 'flex',
                    justifyContent: 'center',
                    marginTop: 100,
                    width: '100vw',
                    height: '100vh'
                }}
            >
                <h1>Blog</h1>
            </div>
        </Layout>
    );
}
